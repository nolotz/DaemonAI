{
  "name": "@daemonai/infra",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "synth": "cdk synth -c stage=dev",
    "deploy:dev": "cdk deploy -c stage=dev --all --require-approval never",
    "deploy:prod": "cdk deploy -c stage=prod --all",
    "destroy:dev": "cdk destroy -c stage=dev --all"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "aws-cdk": "^2.170.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
